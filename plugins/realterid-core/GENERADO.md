# realterid-core — directorio GENERADO

**No edites nada de esta carpeta.** Se regenera entera con:

```bash
node scripts/build-plugins.mjs
```

La fuente de este plugin vive en `src/hooks/` (y las guías compartidas, en `src/guides/`).
Cualquier cambio hecho aquí se pierde en el próximo build.
